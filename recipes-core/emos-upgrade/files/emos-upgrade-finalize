#!/bin/sh

#
# Copyright (C) 2018, TQ Systems
#

set -e

LAST_UPDATE="/update/last-upgrade.raucb"
LAST_UPDATE_TMP="/tmp/last-upgrade.raucb"

mv -f "$LAST_UPDATE" "$LAST_UPDATE_TMP"
rauc install "$LAST_UPDATE_TMP"
rauc status mark-active booted
rm -f "$LAST_UPDATE_TMP"
